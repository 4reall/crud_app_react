import PropTypes from 'prop-types';

import styles from './pagesNavBar.css';

const PagesNavBar = ({ buttonsArray }) => {
	// console.log(buttonsArray);
	// const view = buttonsArray.map((button) => <button>{button}</button>);
	// return <div>{}</div>;
};

PagesNavBar.propTypes = {};

export default PagesNavBar;
