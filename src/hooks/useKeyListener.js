// import {useEffect} from "@types/react";
//
// const useKeyListener = () => {
// 	const keyListener = (key, action) => {
// 		useEffect(() => {
// 			const handler = (e) => {
// 				if (e.key === key) action();
// 			};
//
// 			window.addEventListener('keydown', handler);
//
// 			return () => window.removeEventListener('keydown', handler);
// 		}, []);
// 	}
//  	return {}
// }
